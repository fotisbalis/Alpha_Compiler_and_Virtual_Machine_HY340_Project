/*
 * BASIC ERROR / arithmetic on a non-number
 * Expected: runtime error -> "Arithmetic operation on non-number types"
 * (number + table).
 */
t = [1, 2, 3];
print("about to add a number and a table\n");
x = 5 + t;
print("this line must never run: ", x, "\n");
