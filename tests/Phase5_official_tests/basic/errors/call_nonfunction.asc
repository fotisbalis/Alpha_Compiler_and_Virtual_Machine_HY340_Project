/*
 * BASIC ERROR / calling something that is not callable
 * Expected: runtime error -> "Cannot call non-function type".
 */
x = 5;
print("about to call a number\n");
x();
print("this line must never run\n");
