/*
 * BASIC ERROR / calling a string that names no library function
 * Expected: runtime error -> "Library function '...' not found".
 * Calling a STRING value performs a library-function lookup by that name.
 */
f = "no_such_library_function";
print("about to call a string that is not a libfunc\n");
f();
print("this line must never run\n");
