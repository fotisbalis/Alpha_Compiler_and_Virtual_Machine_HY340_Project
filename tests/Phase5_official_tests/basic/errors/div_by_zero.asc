/*
 * BASIC ERROR / division by zero
 * Expected: runtime error -> "Division by zero" (VM exits with failure).
 * The divisor is held in a variable so the compiler cannot fold it away.
 */
z = 0;
print("about to divide by zero\n");
x = 10 / z;
print("this line must never run: ", x, "\n");
