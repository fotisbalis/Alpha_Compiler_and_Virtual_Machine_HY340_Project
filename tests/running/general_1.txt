a = 2;
b = 3;
c = 5;

sum = a + b;
prod = sum * c;
cmp = prod > 20;

if (cmp) {
	result = prod;
}
else {
	result = 0;
}

i = 0;
acc = 0;
while (i < 3) {
	acc = acc + i;
	i = i + 1;
}

function multiply(x, y, z) {
	local_sum = x + y;
	print(argument(0), "\n");
	print(argument(1), "\n");
	print(argument(2), "\n");
	return x * y * z;
}

mul_result = multiply(a, b, c);
print("mul = ", mul_result, "\n");
print("typeof(mul_result) = ", typeof(mul_result), "\n");
print("typeof(print) = ", typeof(print), "\n");

tbl = [];
tbl[0] = mul_result;
tbl["sum"] = sum;
tbl["flag"] = cmp;

print(tbl[0], "\n");
print(tbl["sum"], "\n");
print(tbl["flag"], "\n");

tbl[0] = nil;
print(tbl[0], "\n");

tbl[1] = missing_value;
print(tbl[1], "\n");
